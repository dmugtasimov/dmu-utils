from setuptools import setup


version = '0.0.1'

setup(
    name='dmu-utils',
    version=version,
    description='DMU Utils',
    packages=['dmu_utils'],
    zip_safe=False,
    platforms='any',
    install_requires=[],
    extras_require={},
    include_package_data=True,
)
